version: '3.8'

services:
  toon-db:
    build: .
    ports:
      - "3000:3000"
    environment:
      - API_KEY=toondb-secure-key
    volumes:
      - ./data:/root/data
    restart: unless-stopped
    container_name: toon-db